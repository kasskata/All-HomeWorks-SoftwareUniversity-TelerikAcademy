using System;

class CoffeMachine
{
    static void Main()
        {
            decimal n1 = 0.05m * (decimal.Parse(Console.ReadLine()));
            decimal n2 = 0.10m * (decimal.Parse(Console.ReadLine()));
            decimal n3 = 0.20m * (decimal.Parse(Console.ReadLine()));
            decimal n4 = 0.50m * (decimal.Parse(Console.ReadLine()));
            decimal n5 = 1.00m * (decimal.Parse(Console.ReadLine()));
            decimal sum = n1 + n2 + n3 + n4 + n5;
            decimal develeper = decimal.Parse(Console.ReadLine());
            decimal price = decimal.Parse(Console.ReadLine());

        // Not completed

        }
}
